std := import('std')
str := import('str')

{ Soak: Soak } := import('src/main')

// ---

test := [
  ['# 1', 0, 1]
  ['# 1', 1, 2]
  ['add', 2, 3]
]

// ---

env := Soak()
env.load(test)
env.jit() |> std.println()
env.Env |> keys() |> len() |> std.println()